{
  "name": "kuepc-backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/loader.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "load": "npm run build && node -r dotenv/config ./dist/loader.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parse": "^5.3.0",
    "dotenv": "^16.0.1",
    "graphql-request": "^4.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.6",
    "typescript": "^4.7.4"
  }
}
